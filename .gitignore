node_modules
.next
.certs
.runtime
dist
coverage
*.log
data/uploads
data/backups
data/email-outbox
.env
.env.development
.env.development.ops
.env.cloud-production
.env.cloud-production.ops
.env.prod
*.tsbuildinfo
rootCA.pem
*:Zone.Identifier

# Screenshots de desenvolvimento
/*.png
/*.jpg

# Playwright MCP snapshots
.playwright-mcp/

# MCP config (local)
.mcp.json

# Claude Code local settings (pode conter credenciais locais)
.claude/settings.local.json
# Lock runtime do agendador interno do Claude Code
.claude/scheduled_tasks.lock

# Fichas PDF geradas via `npm run generate:fichas` (nao versionado)
/ficha-*.pdf

# Cache local de outras ferramentas
.codex

# Arquivos de analise temporaria (CSS audit, scripts ad-hoc)
CSS_ANALYSIS.md
analyze_css.py
extract_css.py

# Anotacoes pessoais (nao versionar)
Opções para pausar.txt
notes-*.md

# Artefato local do gerador de bitmap da etiqueta
print-agent/logo-preview.png

# Artefato local do preview da etiqueta (scripts/preview-custom-label.mjs):
# o principal + as variantes por quantidade de lotes (-2/-6/-14 etc.).
print-agent/custom-label-preview*.png
print-agent/custom-label-preview*.svg
print-agent/shipping-label-preview*.png
print-agent/shipping-label-preview*.svg

# Estado local do print-agent (jobs ja impressos nesta sessao)
print-agent/.printed-jobs.json

# Saidas de scripts de auditoria/wizard (relatorios, JSON dumps, undo SQL)
/tmp/
