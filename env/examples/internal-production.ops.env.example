# Canonical example for host-side operational scripts in `internal-production`.
# Copy this file to `.env.internal-production.ops`.

SMOKE_USERNAME=admin_internal
SMOKE_PASSWORD=replace-with-known-internal-production-password

# Optional override when the operator needs to target another local URL:
# API_BASE_URL=http://127.0.0.1:3001
