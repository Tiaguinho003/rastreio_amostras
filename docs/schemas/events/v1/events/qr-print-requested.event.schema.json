{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://amostras-safras.local/schemas/events/v1/events/qr-print-requested.event.schema.json",
  "title": "QR_PRINT_REQUESTED event",
  "description": "Q.print: impressao virou acao pura. Audit-only (fromStatus=null/toStatus=null) — nao muda mais o status do sample. Estado de impressao vive na tabela PrintJob.",
  "allOf": [
    { "$ref": "../base/event-envelope.schema.json" },
    {
      "type": "object",
      "properties": {
        "eventType": { "const": "QR_PRINT_REQUESTED" },
        "schemaVersion": { "const": 1 },
        "payload": { "$ref": "../payloads/qr-print-requested.payload.schema.json" },
        "fromStatus": { "type": "null" },
        "toStatus": { "type": "null" },
        "idempotencyScope": { "const": "QR_PRINT" },
        "idempotencyKey": { "type": "string", "minLength": 1 }
      },
      "required": [
        "eventType",
        "schemaVersion",
        "payload",
        "fromStatus",
        "toStatus",
        "idempotencyScope",
        "idempotencyKey"
      ]
    }
  ]
}
